# =============================================================================
# CobranzaCloud - Variables de Entorno
# =============================================================================
# Copiar este archivo a .env y ajustar valores para producción
# NUNCA commitear .env con secretos reales
# =============================================================================

# ============ DATABASE ============
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres_dev_password
POSTGRES_DB=cobranzacloud
POSTGRES_PORT=5432

# ============ REDIS ============
REDIS_PORT=6379

# ============ BACKEND ============
ASPNETCORE_ENVIRONMENT=Development
ASPNETCORE_URLS=http://+:5000

# JWT (cambiar en producción - mínimo 32 caracteres)
JWT_SECRET_KEY=super_secret_key_change_in_production_min_32_chars
JWT_ISSUER=CobranzaCloud
JWT_AUDIENCE=CobranzaCloud

# ============ FRONTEND ============
NEXT_PUBLIC_API_URL=http://localhost:5000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============ CLERK (Auth) ============
# Obtener de https://dashboard.clerk.com
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxxxx
CLERK_SECRET_KEY=sk_test_xxxxx

# ============ OPENAI (IA) ============
# Obtener de https://platform.openai.com
OPENAI_API_KEY=sk-xxxxx

# ============ CONNECTION STRINGS ============
# Estas se construyen automáticamente en docker-compose
# Para desarrollo local sin Docker, descomentar y ajustar:
# DATABASE_URL=Host=localhost;Port=5432;Database=cobranzacloud;Username=postgres;Password=postgres_dev_password
# REDIS_URL=localhost:6379
